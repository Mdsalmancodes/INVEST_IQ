# infrastructure layer — repository implementations, external clients
# (Document 2 §5.3). Only cache/redis_clients.py exists in Phase 1 (needed
# for /ready). Mongo repositories, model artifact loading, Celery task
# definitions arrive Phase 7+.
