{
  "name": "investiq",
  "version": "0.1.0",
  "private": true,
  "description": "INVEST IQ — AI Powered Investment Intelligence Platform (monorepo root)",
  "packageManager": "pnpm@9.12.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:unit": "turbo run test:unit"
  },
  "devDependencies": {
    "turbo": "2.1.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
