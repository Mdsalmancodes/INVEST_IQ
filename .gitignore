# Dependencies
node_modules/
.pnpm-store/
__pycache__/
*.pyc
.venv/
venv/

# Build outputs
.next/
dist/
build/
*.egg-info/

# Env files (never commit secrets — see .env.example for the template)
.env
.env.local
.env.*.local
!.env.example

# Turbo
.turbo/

# Testing / coverage
coverage/
.coverage
htmlcov/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# IDE
.vscode/*
!.vscode/extensions.json
.idea/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
dev_log.txt
server_log*.txt

# Local dev scratch environment (vendored Postgres/Redis binaries, live DB
# data directories, manual curl/browser test dumps) — never committed;
# see docs/architecture/ for the real docker-compose based local setup.
.localdev/

# TypeScript incremental build cache
*.tsbuildinfo

# Playwright / e2e test output
test-results/
playwright-report/

# ai-service local model-training scratch artifacts (never committed —
# real artifacts are stored per Document 4 §10.8)
apps/ai-service/data/test-models/

# Docker volumes data (if bind-mounted locally)
infra/docker/volumes/

# ML artifacts (never committed — stored in object storage per Document 4 §10.8)
ml/models/*.pkl
ml/models/*.h5
ml/models/*.pt
ml/models/artifacts/

# Alembic (keep migration scripts, ignore local sqlite test DBs if any)
*.sqlite3

# Storybook build
storybook-static/
